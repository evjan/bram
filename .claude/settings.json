{
  "permissions": {
    "allow": [
      "Write(./resources/worklist.json)",
      "Edit(./resources/worklist.json)",
      "Bash(curl -s \"http://localhost*__worklist*)",
      "Bash(curl -s -X POST -d * \"http://localhost*__worklist*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/worklist-guard.py"
          }
        ]
      }
    ]
  }
}

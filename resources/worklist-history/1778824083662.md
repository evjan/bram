# Worklist change @ 2026-05-15T05:48:03Z (1778824083662)

**Summary:** 0 proposed, 0 advanced, 0 renamed, 2 pruned

## Description changed

Was: `Roll the dual-readership framing + multi-file diff fix into v0.1.14: documentation lands, then schema extension makes TO COMMIT review honest, then cut the release.`
Now: `Cut v0.1.14: first end-to-end release on top of the worklist-history feature.`

## Items pruned (committed or dropped)

- `document-dual-readership-framing` (was applied, ``)
  - **Before:** The worklist-history feature serves two readers: humans during active triage of items, and future agents grepping for forensic context after the original session is gone. Neither place in the codebase currently makes this explicit:  - `app/__shell/conventions.md` is silent on how to write item prose. An agent drafts `before`/`after` based on whatever feels right in the moment, which often leans toward the immediate human reader and skips alternatives considered. - `docs/worklist-history.md` describes the mechanism (flow, phases, routes, UI) but never says who the artifact is FOR. A reader doesn't learn why specificity in the prose earns its keep.  Reverts in this very session — the commit-correlation feature, the per-item lifecycle filter — are now invisible in the squashed git log; their only trace was in snapshot prose that we discarded as dev-era artifact. A future agent wondering 'did we consider audit-trail correlation?' has worse retrieval than they should.
  - **After:** Two-file change:  (1) `docs/worklist-history.md` — add a short 'Who reads this' section near the top of the doc, capturing the dual-readership story. Humans use the artifact during active triage; agents lean on it later as forensic audit trail. The implication for prose authors: write `before`/`after` for the long-tail reader, name alternatives considered (not just the chosen path), and assume the agent grepping months from now does not have access to the conversation that produced the item.  (2) `app/__shell/conventions.md` — under the worklist section (near 'When to route through the worklist'), add a brief authorship rule: 'Write item prose with the future-agent reader in mind. Name alternatives considered, not just the chosen path. The agent who reads this snapshot months from now does not have the conversation that produced it — the worklist history is their only retrieval.' Conventions get auto-loaded into every agent's context, so this rule actively shapes how items get drafted from this commit forward.  Alternatives considered: (a) doc-only change, no convention update — rejected because the doc is reference material, not behavioral guidance; without the convention line, the rule doesn't propagate. (b) Convention-only, no doc update — rejected because the convention rule cites a framing that's then nowhere expanded. (c) Single combined paragraph in just one file — rejected because the two readers (agent loading conventions, human reading the doc) want different framings of the same point.  Commit message: 'docs: frame worklist-history as agent-forensic audit trail'.
- `multi-file-diff-support` (was applied, ``)
  - **Before:** Worklist items carry a single `file: <string>` field, used by the /__worklist route to compute the inline diff shown in TO COMMIT rows. Reality is that many feature changes touch 2+ files — core feature was lib.rs + Workspace.xmlui; the framing item is conventions.md + docs/worklist-history.md; track-worklist-history was doc + snapshot folder. The current schema forces the agent to pick one as 'the primary' and describe the others in prose; the inline diff then renders only that one file, leaving the user to verify the rest via terminal git diff or by asking the agent to paste the diff into chat. Surfaced sharply on the framing item: two files touched, inline diff shows one, user reports 'this is pretty confusing.'  Alternatives considered:  (a) Status quo + verify in chat. Agent dumps `git diff` for unrendered files when asked. Cheap but breaks the principle that TO COMMIT review should be self-contained. Rejected because it's the current source of confusion.  (b) Render the full unstaged tree diff per applied item, no path filter. Simple but all applied items would show the SAME diff, breaking per-item attribution when multiple items sit at TO COMMIT simultaneously. Rejected earlier in this session.  (c) Multi-file via a `files: [...]` array, fallback to `file` for backward compat. Each item declares its scope honestly; route fetches and concatenates diffs; the existing line-by-line renderer handles the combined output unchanged (file boundaries are visually distinct via the `diff --git a/...` header lines). Chosen because it expresses item scope correctly without forcing a global rewrite of the schema, and the agent is the one choosing the file list — same prediction burden as today's `file:`, just plural.
  - **After:** Three-file change:  (1) `src-tauri/src/lib.rs` (the /__worklist route handler): replace the single-file diff fetch with a loop over `files` (when present) or `[file]` (backward compat). For each path, run `git diff -- <path>`, falling through to `git diff --no-index -- /dev/null <path>` for untracked files (the existing fallback). Concatenate the resulting diffs with a single newline separator; inject as the item's `diff` field. The Workspace.xmlui renderer needs no change — it splits the diff string on newlines and colors each line by prefix, and the `diff --git a/...` headers between files render as uncolored separator lines that visually delineate per-file sections.  (2) `app/__shell/conventions.md`: update the worklist item schema documentation to mention `files: [...]` as an alternative to `file: <string>`. One-liner about when to prefer it (any change spanning 2+ files).  (3) `resources/worklist.json`: after the route change lands and the binary is rebuilt, edit the in-flight `document-dual-readership-framing` item to use `files: ["docs/worklist-history.md", "app/__shell/conventions.md"]` instead of single `file`, so its TO COMMIT inline diff shows both files together — turning the framing-item's confusion into the demo we wanted.  Commit message: 'worklist: support multi-file scope on items'.

